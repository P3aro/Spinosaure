#include <stdlib.h>
#include <stdio.h>
#include <time.h>

int rand(void);

void    ft_moreless(int nbr)
{
    int nbr = 0;
    int i = 0;
    int mystere = 0
    for (i = 0, i >=0 && i <= 100, i++)
    {
        mystere = rand ();
    while (nbr != i)
    {
        scanf("%d", &nbr)
        if (nbr < i)
            printf("%s", "c'est plus!");
        else
            printf("%s", "c'est moins!");
    }
    printf("%s", "Bravo, vous avez trouve le nombre mystere !!!");
}

int main()
{
    ft_moreless();
    return (0);
}
